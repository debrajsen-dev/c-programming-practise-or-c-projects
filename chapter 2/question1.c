#include <stdio.h>

int main(){
    /*
    3. Write a program to check whether a number is divisible by 97 or not. 
    */                                            
          int a= 22;
          int b=97;
          printf("If the reminder is 0 then it is divisible by %d, the reminder %d ",b,b%a);
    return 0;
}